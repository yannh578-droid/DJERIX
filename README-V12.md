# DJERIX V12 — lancement local

Dans Terminal :

cd /Users/admin/Desktop/DJERIX-V12-FIXED/DJERIX-V5/server
npm install
npm start

Puis ouvrir http://localhost:3000

Créer `server/.env` :
OPENAI_API_KEY=VOTRE_CLE
DJERIX_MODEL=gpt-5.6-luna
PORT=3000

Ne jamais publier `.env`.
